{
  "name": "remote-terminal-server",
  "version": "0.8.0",
  "private": true,
  "description": "Relay for Remote Terminal (protocol v3): enrolment, pairing, device tokens, multi-agent session routing, presence, limits.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
