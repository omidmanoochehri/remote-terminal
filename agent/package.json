{
  "name": "remote-terminal-agent",
  "version": "0.7.1",
  "private": true,
  "description": "Cross-platform (Windows / Linux) agent that hosts persistent terminal sessions (PTY) for the Remote Terminal relay, protocol v3.",
  "main": "index.js",
  "type": "commonjs",
  "bin": { "remote-terminal-agent": "index.js" },
  "scripts": {
    "start": "node index.js",
    "test": "node --test --test-timeout=60000"
  },
  "dependencies": {
    "@homebridge/node-pty-prebuilt-multiarch": "^0.13.1",
    "ws": "^8.18.0"
  }
}
