<?xml version="1.0" encoding="utf-8"?>
<!-- Informational note card (the security explainer on Add machine). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/rt_accent_soft" />
    <stroke android:width="@dimen/rt_stroke" android:color="@color/rt_outline" />
    <corners android:radius="@dimen/rt_radius_card" />
</shape>
