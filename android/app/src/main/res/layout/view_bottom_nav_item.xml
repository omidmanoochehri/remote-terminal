<?xml version="1.0" encoding="utf-8"?>
<!-- One destination in the floating bottom navigation bar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingTop="15dp"
    android:paddingBottom="10dp"
    android:background="@drawable/rt_nav_item"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/navIcon"
        android:layout_width="@dimen/rt_icon_lg"
        android:layout_height="@dimen/rt_icon_lg"
        android:importantForAccessibility="no"
        app:tint="@color/rt_text_dim"
        tools:src="@drawable/ic_rt_home"
        xmlns:tools="http://schemas.android.com/tools" />

    <TextView
        android:id="@+id/navLabel"
        style="@style/RtText.NavLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
