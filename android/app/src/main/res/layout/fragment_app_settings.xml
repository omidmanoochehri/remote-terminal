<?xml version="1.0" encoding="utf-8"?>
<!--
  App settings. Sections and rows are built in code from one row component so
  every preference in the app is presented the same way and none of them had
  to be dropped to fit the design.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/headerBar"
        layout="@layout/view_screen_header" />

    <include
        android:id="@+id/searchBar"
        layout="@layout/view_search_field"
        android:layout_width="match_parent"
        android:layout_height="43dp"
        android:layout_marginBottom="4dp"
        android:visibility="gone" />

    <androidx.core.widget.NestedScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/rt_nav_height">

            <LinearLayout
                android:id="@+id/sections"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="@dimen/rt_gutter"
                android:paddingEnd="@dimen/rt_gutter" />

            <include
                android:id="@+id/stateBlock"
                layout="@layout/view_state_block" />
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
